name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4

      - name: 2. Set up Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 3. Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.x'
          channel: 'stable'

      - name: 4. Generate Android Project Structure
        run: |
          flutter create --project-name bmi_health_tracker --org com.renantefullo . --platforms android

      - name: 5. Install Dependencies
        run: flutter pub get

      - name: 6. Build APK Release
        run: flutter build apk --release

      - name: 7. Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Health-Suite-App-Release
          path: build/app/outputs/flutter-apk/app-release.apk
